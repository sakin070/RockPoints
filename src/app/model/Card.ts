export class Card {
  // id: string;
  cardNumber: number;
  pointsCollected: number;
  // firstName: string;
  // lastName: string;
  constructor(cardNumber: number, pointsCollected: number) {
    // this.id = '';
    this.cardNumber = cardNumber;
    this.pointsCollected = pointsCollected;
  }
}
